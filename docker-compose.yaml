version: '3.8'

services:
  search-api:
    build: .
    container_name: python-saas-search
    ports:
      - "${EXTERNAL_PORT}:8000"
    restart: always
    # Опционально: прокси, если DuckDuckGo начнет блокировать
    # environment:
    #   - HTTP_PROXY=http://your-proxy:port